# Testing

This project was generated using [Nx](https://nx.dev).

## Running unit tests

- Run `nx test spa` to execute the frontend unit tests via [Jest](https://jestjs.io).
- Run `nx test backend` to execute the backend unit tests via [Jest](https://jestjs.io).
